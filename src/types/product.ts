export type Categories = {
    id: number,
    title: string,
    description: string,
    isActive: boolean
}
export type Stories = {
    id: number,
    title:string,
    author: string
    image: string
    description: string,
    isActive: boolean
}